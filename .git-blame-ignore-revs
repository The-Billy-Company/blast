# git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add full commit SHAs for tree-wide formatter or migration commits below.
# Blame can then step through mechanical reflow to the change that meant it.
#
# Empty on purpose. This history was audited for the shape blame mistakes -
# broad, balanced churn where lines were rewritten without being rethought -
# by pairing each removed line with its replacement and asking what fraction
# differ by a single bare token. Nothing here clears the bar; blast is young
# enough that its one rename sweep carried real prose along with it.
